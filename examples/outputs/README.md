# Examples Output Directory

This directory is created automatically when you run the example scripts.

Generated outputs are organized by example:
- `full_pipeline/` - Outputs from the full sidewalk generation pipeline
- `protoblocks/` - Outputs from the protoblock generation examples

All output files are ignored by git (see `.gitignore`).
